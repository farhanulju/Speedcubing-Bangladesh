# Build-time public vars (NOT secrets — site keys are public). Copy to .env for local dev.
# Values below are Cloudflare's documented test keys (always pass). Production uses the
# real site key from the Turnstile dashboard (plan/OPS_HANDOFF.md WP-00).
PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA
