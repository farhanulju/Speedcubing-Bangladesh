# CLAUDE.md — Speedcubing Bangladesh

Mirror of `AGENTS.md`. Claude Code reads this file; every other agent reads `AGENTS.md`. Keep the two in sync by hand.

> Canonical rules live in `AGENTS.md`. If the two ever disagree, `AGENTS.md` wins and the mismatch must be fixed immediately.

See `AGENTS.md` for the full contract, then continue with `docs/START_HERE.md`.
